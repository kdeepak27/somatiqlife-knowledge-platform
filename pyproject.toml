[project]
name = "somatiq-knowledge-platform"
version = "0.1.0"
description = "SOMatiq health knowledge-base generation pipeline"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
